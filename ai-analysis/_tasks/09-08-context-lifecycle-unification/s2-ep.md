# S2 子 EP：座標系擴充——pilot 最小條目化

> **ep_type**: implementation（docs/manifest mode——frontmatter 欄位＋desc 改寫＋取用路徑，無 production 行為碼；無 TDD）
> **parent**: ai-analysis/_tasks/09-08-context-lifecycle-unification/ep.md（master blueprint，S2 段）
> baseline: 28734c5

## Context

依 S1 勝出方案，只做 S3 pilot 所需最小條目化；其餘 rules/skills 廣泛改寫列擴展範圍，待 S3/S4 行為與生命週期驗收後展開，不預先全量改寫。文法用 S1 定案的可執行部分（禁引號待確認條款懸置，不批次套用）。

## 做什麼

1. 對象：S3 pilot 路徑必需的 rules（以必要常駐集合反推，預期為子集非 16 條全改）——frontmatter 增 rank＋觸發 desc 欄位（harness-scope 已在場，兩軸合體）；desc 情境句化、rank 僅排序（初判）。
2. on-demand 級內容確認下沉 skill（條目留指針＋可驗取用路徑，不可只搬字）。
3. 確立必要常駐集合、適用層與 scope 的書面清單（S1 初判→本段定稿，供 S3 投影器直接消費）。
4. floor 尺寸須符合各端預算；縮減幅度由必要知識保全決定，不設獨立瘦身目標。

## 驗收

必要集合與入口清單明確、已裁定文法抽查通過、S3 最小投影可承接（交接檢查：S3 子 EP 開工前對本產出打勾）。回滾：改前 frontmatter/desc 快照進任務家 version 目錄。

## 產出

` s2-report.md`（改寫清單＋集合定稿＋擴展範圍登記）——改寫 diff 即證據，不另設 probe。

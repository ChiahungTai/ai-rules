---
name: dispatch-reread-governing-docs
description: 派發/決策當下重讀治理檔——session 記憶對快速演進域過期；看 commit 標題≠重讀條文；startup memory
  舊教義會強化錯誤（codex 收法事故）
metadata:
  node_type: memory
  type: feedback
  originSessionId: sess_3380ab28-4197-4433-aa66-e2f54587b64b
---

09-05 codex 收法事故：派發 codex ep-review 用了已被刪除的舊詞「wrapper＋resume-to-poll」——SendMessage 叫 wrapper 輪詢被其契約明文拒絕（thin forwarder 禁 poll）。事實上治理條文（agents/AGENTS.md dispatch face）**四小時前**已被 4b1950d 改寫得完全精確（主 session 背景 Bash 直呼 companion 阻塞形）——我在撞號調查時親眼讀過該 commit 標題還寫進了 EP baseline 註記，卻沒在派發前重讀那節條文；且 startup context 的 memory 條目仍載舊教義（同步回報）持續強化錯誤模式。第二次照現行條文做（companion status --wait 背景 push）一次成功。

**Why**：user 問「為何你第一次沒做好？ai rules 寫得不夠清楚？」——鑑定結論條文無錯、錯在消費端：長 session 的既存知識對快速演進治理域（收法/registry/閘門類）過期；「知道有改版」與「條文已入腦」是兩件事；memory 舊教義是加倍風險面。

**How to apply**：①派發外部 runtime／引用收法類知識時，決策當下 Read 對應 skill 節原文，不信 session 記憶或 memory desc；②看到「收法/閘門/registry 已改版」的 commit 標題＝重讀觸發器不是已知訊號；③修復已落地：model-routing skill 套用段防護行＋memory 條目更新＋flow-feedback `dispatch-stale-doctrine-reread`（歸檔 _done/，開放問題：重讀紀律是否通用化到 hooks/bundle/registry 域——留 flow-review）。同族：[[feedback_relay-claims-verify-current-state]]（外部 relay 過時）與本條（自身 session 知識過時）互補。

相關：[[reference_external-runtime-delegation-family]]（push 收法現值）、[[feedback_capability-tier-not-model-binding]]

# 紙上 dry run：UC6 fresh machine 重建 ＋ UC7 新 repo onboarding

- 問題：文檔宣稱的步驟在實際檔案/工具面能否走通（斷點走查）
- 軸：git/repo 檔案面（含機器本地現況對照）；方法＝逐項 rg/Read/ls 實證
- 日期：2026-09-10；工作目錄 /Users/ctai/Github/ai-rules；read-only（唯一寫入=本檔）

---

## UC6：fresh machine 重建（硬碟壞了，新機器從零恢復）

| # | 項目 | 判定 | 證據（file:line / 命令） | 斷點一句 |
|---|------|------|--------------------------|----------|
| 1 | git clone＋基本工具（uv/node/backlog CLI/npm global）onboarding | ❌ | repo root 無 README.md（ls 實測；fd README* 僅 ai-analysis/README.md＝分析側工作區說明）；backlog CLI 機器現值 npm global `backlog.md@1.50.1`（npm ls -g），rg 全 repo 無安裝文檔；MULTI-MACHINE.md:5 只列池移植子集「bash/coreutils/jq/python3——新機裸環境未必有 uv」 | 無統一 fresh-machine onboarding 文檔；基礎工具鏈（uv/node/npm-global backlog/jq）安裝清單零記載 |
| 2 | rules/skills 部署＋全域 symlink | ⚠️ | deploy ✅：scripts/deploy_agents.py:1-45 自描述（目標/scope/size gate/idempotent）＋rules/AGENTS.md:12-42 部署紀律＋/sync-sources 機械檢查（:40）。Claude symlink ⚠️：deploy_agents.py:11-12 明言不碰 Claude 端；佈局記載 rules/AGENTS.md:9（CLAUDE.md 檔案 symlink→guide、rules/ 目錄 symlink）；機器在場（ls -la ~/.claude/）；無建立腳本。skills ❌：~/.zcode/skills/ 與 ~/.agents/skills/ 是實體複製（ls -la 實測 82 目錄非 symlink）；repo 無腳本產生（rg scripts/ hooks/ deploy/ 零命中）；記載僅歷史報告 cross-harness-commands-skills-deployment.md:41,118（手動 import）＋04 報告:119 自標「未驗證」。agents symlink ⚠️：agents/AGENTS.md:3 記載 ~/.zcode/agents→agents/zcode/（機器在場）；建立無腳本 | deploy 腳本面完整；但 Claude/agents symlink 無腳本僅佈局描述，skills 對 ~/.zcode/skills・~/.agents/skills 的 82 目錄複製**無腳本無現行 runbook**，重灌靠逐個手動 import |
| 3 | hooks 接線（zcode-registration merge、CC settings） | ⚠️ | ZCode ✅：hooks/zcode-registration.json `_comment` 四條＋hooks/AGENTS.md merge 語義（events 子樹 merge 進 hooks: 鍵、_comment 不隨行、SessionEnd 現版不可 merge）。muse ✅：.muse/hooks.json machine-local 生成物、setup-muse-hooks.sh:1-7 自描述 re-run after clone。CC ⚠️：hooks/AGENTS.md memory sensors 段描述註冊條目（PostToolUse/FileChanged/SessionStart）；但 repo settings.json **gitignored**（.gitignore:36-37「contains API keys」）——fresh clone 後不存在 | ZCode/muse 接線可照文檔重建；CC settings.json 含 API keys 不進版控（設計如此），hooks 條目可手建但 **API keys 無 repo 源**、需外部備份，文檔未提示此恢復依賴 |
| 4 | 記憶池恢復（bundle restore） | ✅ | hooks/MULTI-MACHINE.md 完整 runbook：傳池二選一（bundle `git clone <bundle> <repo>/.agents/memory/`／cp -a 保 mtime，:9-13）；setup-memory-symlinks.sh（dry-run 預設/--apply/.bak，:14-23）；verify-memory-topology.sh（只讀+--smoke，:35-40）。bundle 在場：~/.agents/memory-bundles/ ai-rules-2026-09-09.bundle＋mosaic-2026-09-10.bundle（ls 實測）；輪替 7 份由夜間 cron #1 承載（schedule-registry.md:15） | 無（唯一完整覆蓋的 UC6 項）；註：~/.agents/memory-spine/ 在池外、bundle 不涵蓋（見彙總 #7） |
| 5 | cron/launchd 重建 | ⚠️ | registry ✅：schedule-registry.md:13-19 三條 ZCode cron automationId 全名+時刻+職責+角色紅線；:23-26 launchd 兩服務；A1-A6 反查表。MULTI-MACHINE.md §4 自標「重建排程（最易漏）」但只說要建、不指內容源。cron prompt 本體 ❌：design.md:134-135／09-03 ep.md:135 只錄 automationId+職責一句；rg 全 repo 無 CronCreate prompt 全文（design.md:249 僅同步義務）；prompt 現值只存 ZCode 本機 DB，且 AIR-14 L8/AIR-52 S4/AIR-54 P5 三次 CronUpdate 修訂散多檔。launchd ⚠️/❌：browser plist 版控 ✅（git ls-files 有 deploy/launchd/com.ai-rules.backlog-browser.plist）；**cleanup plist 未版控**（git ls-files 無；機器在場 ~/Library/LaunchAgents/）——scan report:170 F1#8 已標＋air-70 卡（To Do）承接；啟用命令 ❌：launchctl bootstrap/load 無現行文檔（rg 僅 archived ep-code-reality-repo-mcp.md:359 且屬別的服務） | 排程斷點最重：cron prompt 本體無 verbatim 備份（重建=多檔拼裝難保逐字）；cleanup plist 未版控（已有卡）；plist 啟用命令無文檔；mosaic twin plist 跨 repo 依賴僅指針 |
| 6 | code-reality binary 安裝 | ✅ | skills/code-reality/SKILL.md:19：uv tool install 三件組（code-reality+pyrefly-producer+code-reality-lsp-bridge）落 ~/.local/bin；rust-analyzer=rustup component add；plugin wrapper 首 session 自動 bootstrap（pin 比對、missing 即 uv tool install --force、無 uv loud 127+指引）；cargo dev face；「安裝/wrapper 細節真相源＝CR plugin/README.md」 | 無 |

## UC7：新 repo onboarding（方法論裝到新 repo）

| # | 項目 | 判定 | 證據 | 斷點一句 |
|---|------|------|------|----------|
| 1 | backlog 初始化 | ✅ | skills/kanban-board/SKILL.md:15-21「初始化（repo 首次採用）」：`backlog init "<project>" --agent-instructions none`（含理由）＋config.yml 關鍵鍵慣例（statuses 三欄/task_prefix/auto_commit: false/check_active_branches 多 WT 必開） | 無 |
| 2 | 任務家目錄結構 | ✅ | ai-analysis/README.md:5-14 本 repo 目錄角色表；放置單一源 skills/_common/illustrate-html-mode.md:83-87「產物位置分流」含任務家探測：`<repo>/ai-analysis/_tasks/` 在場→雜項任務家、線任務 `_projects/<線>/tasks/`、否則 repo-root `00-tasks/` fallback、歸檔 done/ 探測規則——新 repo 兩條路徑皆有定義 | 無 |
| 3 | 記憶池新池 bootstrap | ⚠️ | 組件有記載：generator「部署＝複製進各專案 memory dir」（memory-audit SKILL.md:35；資產源 skills/memory-audit/scripts/generate_index.py 實測在場）；A/B 形態判準（_resident-set.md 在場=B、無清單維持 A、per-pool opt-in）；_audit-state 首次 audit 寫入（SKILL.md:109「欄位缺＝首次：本次寫入」）；memory_telemetry.py 在場 | 無「新 repo 建池」step-by-step 段（headings 全列無 bootstrap 節）：mkdir 池→generator 部署→首次 regen→B 形態 _resident-set.md（user 凍結物）初建者與程序未定義 |
| 4 | hooks opt-in | ⚠️ | hooks/AGENTS.md 逐 hook 記載行為+gating；sensors 池判定＝父目錄含 MEMORY.md（天然 self-gating，新 repo 無池即 no-op 安全）；zcode-registration=user-level 全域（一次裝、全 repo 生效，非 per-repo opt-in） | muse hooks.json 是 per-repo：setup-muse-hooks.sh 的 REPO 取自腳本自身位置（dirname $0/..）——新 repo 採用＝複製腳本到新 repo hooks/，此步驟無文檔（MULTI-MACHINE.md 只涵蓋同 repo 換機）；無「新 repo hook 佈署清單」 |
| 5 | code-reality（.code-reality.toml、build） | ✅ | skills/code-reality/SKILL.md:68 repo profile（.code-reality.toml——repo 擁有）生態示例段；:109 profile 落被掃 repo root（untracked 屬設計）、sidecar 落 <repo>/.code-reality/ 自帶 gitignore、非 NT 佈局 boundary_build crash loud 屬設計先 smoke；description:3「新 repo 數據面一鍵準備（build）」 | 無 |
| 6 | AGENTS.md 專案檔模板 | ✅ | skills/instruction-init/SKILL.md：為新/unfamiliar codebase 從零產生 root+模組雙檔（AGENTS.md source+CLAUDE.md @wrapper）、bottom-up、Truth-Anchored 變體 | 無 |

---

## 彙總：斷點清單

### 已有卡覆蓋
1. `com.ai-rules.backlog-cleanup.plist` 未版控 → **air-70**（To Do）承接 F1#8（ai-analysis/reports/2026-09-10-repo-consistency-scan.md:170）

### 無卡覆蓋（新發現，建議開卡或併入既有弧）
1. **UC6-1** fresh-machine 統一 onboarding 文檔缺：uv/node/npm-global backlog CLI/jq 安裝清單零記載（MULTI-MACHINE.md:5 僅池移植子集）
2. **UC6-2** skills 佈署無腳本無現行 runbook：~/.zcode/skills・~/.agents/skills 82 目錄實體複製，重灌靠 ZCode 手動 import（記載僅歷史報告、一處自標 stale）
3. **UC6-2** Claude/agents symlink（~/.claude/CLAUDE.md、~/.claude/rules、~/.zcode/agents、~/.claude/agents）無建立腳本；佈局有記載（rules/AGENTS.md:9、agents/AGENTS.md:3）但無 onboarding 步驟串接
4. **UC6-3** CC settings.json（gitignored 含 API keys）fresh machine 無恢復源；hooks 條目可照 hooks/AGENTS.md 手建，keys 需外部備份——文檔未提示此依賴
5. **UC6-5** ZCode cron prompt 本體無 repo verbatim 備份：registry 只錄 automationId+職責一句，三次 CronUpdate 修訂散多檔——schedule-registry 不足以逐字重建排程
6. **UC6-5** launchd 啟用命令（launchctl bootstrap/load）無現行文檔（僅 archived EP 一處、屬別的服務）
7. **UC6-4 邊界** ~/.agents/memory-spine/（跨池 spine）在池外、bundle 不涵蓋，MULTI-MACHINE.md 未提其重建；現值僅 index.md（損失面小，結構性缺口）——spine 是否另有備份未驗證（rg 無命中）
8. **UC7-3** memory-audit 無「新池 bootstrap」段：generator 部署後首次 regen、B 形態 _resident-set.md（user 凍結）初建程序未定義
9. **UC7-4** muse hooks.json per-repo 採用（複製 setup-muse-hooks.sh 到新 repo）無文檔

### 觀察（非斷點）
- UC7 整體覆蓋顯著好於 UC6：六項中四項 ✅——「擴張到新 repo」的路徑多數已 skill 化；「機器災難重建」是缺口集中區（機器本地態：cron DB、skills 複製、settings.json、prompt 本體）
